---
phase: 02-static-content-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - app/page.tsx
  - app/_components/Header.tsx
  - app/_components/Footer.tsx
  - app/_components/CTAButton.tsx
autonomous: true

must_haves:
  truths:
    - "Page has a fixed/sticky navigation header with site branding and CTA link"
    - "Page has a footer with contact info, disclaimer link, and copyright"
    - "Design system defines premium color palette, typography, and spacing tokens via Tailwind @theme"
    - "Layout wraps all page content with consistent header and footer"
  artifacts:
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme with full color palette, fonts, and spacing tokens"
      contains: "@theme"
    - path: "app/layout.tsx"
      provides: "Root layout with SEO metadata, fonts, and page shell"
      contains: "metadata"
    - path: "app/_components/Header.tsx"
      provides: "Navigation header component"
      contains: "Header"
    - path: "app/_components/Footer.tsx"
      provides: "Footer with contact and legal info"
      contains: "Footer"
    - path: "app/_components/CTAButton.tsx"
      provides: "Reusable CTA button component (Client Component for smooth scroll)"
      contains: "use client"
  key_links:
    - from: "app/layout.tsx"
      to: "app/_components/Header.tsx"
      via: "import and render in layout body"
      pattern: "import.*Header"
    - from: "app/layout.tsx"
      to: "app/_components/Footer.tsx"
      via: "import and render in layout body"
      pattern: "import.*Footer"
    - from: "app/globals.css"
      to: "all components"
      via: "Tailwind @theme tokens consumed by utility classes"
      pattern: "--color-primary"
---

<objective>
Establish the design system, page shell (header + footer), SEO metadata, and reusable CTA button for the Reiki landing page.

Purpose: Every subsequent plan needs the design tokens, layout shell, and shared components. This is the foundation that all content sections render inside.
Output: globals.css with premium theme, layout.tsx with SEO metadata and header/footer, reusable CTAButton component, Header and Footer components.
</objective>

<execution_context>
@C:\Users\Kamal\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Kamal\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-static-content-seo/02-CONTEXT.md
@.planning/phases/02-static-content-seo/02-RESEARCH.md
@.planning/phases/01-foundation-configuration/01-01-SUMMARY.md

# Current source files to modify
@app/globals.css
@app/layout.tsx
@app/page.tsx
@next.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design system and global styles</name>
  <files>app/globals.css</files>
  <action>
Replace the current globals.css with a premium Reiki-themed design system using Tailwind CSS v4 @theme directive. This is a landing page for a healing/wellness practice, so the palette should feel calm, premium, and trustworthy.

Design tokens to define in @theme:

**Colors (use oklch for modern color space):**
- primary: A calming blue-teal palette (50 through 900) -- conveys trust, healing, calm
- accent: A warm gold/amber for CTAs and highlights (400, 500, 600) -- draws attention, conveys premium
- neutral: Warm grays (50 through 900) -- softer than pure gray for wellness feel
- surface: Background variants (white, cream/off-white for alternating sections, dark for footer)

**Typography:**
- --font-heading: serif font (e.g., 'Playfair Display') for headings -- premium, elegant feel
- --font-body: sans-serif font (keep Geist Sans from Phase 1 setup, or use Inter) for body text -- clean, readable

**Spacing:**
- --spacing-section: 5rem (80px) for section vertical padding
- --spacing-section-lg: 7rem for larger sections (hero)

**Additional custom styles:**
- Remove dark mode media query (this is a light-theme-only landing page)
- Add a smooth scroll behavior on html element
- Define a `.section-container` utility in @layer utilities for max-w-7xl mx-auto px-4 sm:px-6 lg:px-8
- Add `.text-balance` utility for balanced text wrapping on headings

Keep the @import 'tailwindcss' at the top (Tailwind v4 syntax established in Phase 1). Do NOT use @tailwind directives.
  </action>
  <verify>Run `npm run build` from project root. Build must succeed with zero errors. Verify the CSS file contains @theme directive with --color-primary, --color-accent, --font-heading tokens.</verify>
  <done>globals.css contains a complete @theme block with primary (9 shades), accent (3 shades), neutral (9 shades), surface colors, heading + body font families, section spacing tokens, and utility classes. No dark mode. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Layout shell with SEO metadata, Header, Footer, and CTA button</name>
  <files>
    app/layout.tsx
    app/_components/Header.tsx
    app/_components/Footer.tsx
    app/_components/CTAButton.tsx
    app/page.tsx
  </files>
  <action>
**Header.tsx (Server Component):**
Create a sticky/fixed header navigation component:
- Sticky at top with backdrop blur and semi-transparent background (becomes solid on scroll -- but since this is a Server Component, keep it simple with CSS-only: use `sticky top-0 z-50 bg-white/80 backdrop-blur-md`)
- Left: Site brand text "Reiki Practice" using heading font
- Right: Navigation links as anchor tags scrolling to sections: "About", "How It Works", "Benefits", "Pricing", "Contact"
- Far right: CTA button "Schedule Consultation" (use the CTAButton component, styled smaller for header)
- Responsive: On mobile, show a hamburger menu -- BUT since this is a Server Component and we want to keep it simple for now, use a simple horizontal scroll or hide nav links on mobile and just show brand + CTA. Do NOT add 'use client' to Header. Keep the mobile menu deferred to Phase 4 (Animations & Polish). For now, hide nav links on mobile (hidden md:flex) and show only brand + CTA.
- Use semantic `<header>` and `<nav>` elements
- Add `aria-label="Main navigation"` to nav

**Footer.tsx (Server Component):**
Create a footer component:
- Dark background (neutral-900) with light text
- Three-column layout on desktop (single column on mobile):
  - Column 1: Brand name, brief tagline, copyright year (dynamic via new Date().getFullYear())
  - Column 2: Quick links (same as nav: About, How It Works, Benefits, Pricing, Contact)
  - Column 3: Contact info placeholder (phone, email -- use placeholder text like "+7 (XXX) XXX-XX-XX" and "info@reiki-practice.com")
- Bottom row: Scientific disclaimer text: "Reiki is a complementary practice and is not a substitute for professional medical treatment. Please consult your healthcare provider for medical concerns."
- Use semantic `<footer>` element

**CTAButton.tsx (Client Component -- 'use client'):**
Create a reusable CTA button:
- Props: `text?: string` (default "Schedule Consultation"), `targetId?: string` (default "pricing"), `variant?: 'primary' | 'header' | 'hero'` (default "primary")
- On click: smooth scroll to the element with id matching targetId
- Styling variants:
  - `hero`: Large button, accent background (gold/amber), white text, px-8 py-4, rounded-lg, text-lg, hover effect (slight scale + darker shade)
  - `primary`: Medium button, accent background, white text, px-6 py-3, rounded-lg
  - `header`: Smaller, accent background, white text, px-4 py-2, rounded-md, text-sm
- Include focus-visible ring for accessibility
- Use Tailwind classes directly (no CSS modules)

**layout.tsx updates:**
- Update the metadata export with comprehensive SEO:
  - title.default: "Reiki Healing | Find Balance and Inner Peace"
  - title.template: "%s | Reiki Healing"
  - description: Compelling 150-160 char description mentioning Reiki healing, sessions, wellness, certified practitioner
  - keywords: ["reiki", "healing", "energy healing", "wellness", "meditation", "stress relief", "holistic health"]
  - openGraph: type "website", title, description, images (placeholder path /opengraph-image.jpg)
  - twitter: card "summary_large_image", title, description
  - Keep existing metadataBase logic (VERCEL_PROJECT_PRODUCTION_URL)
- Import and load heading font (Playfair Display from next/font/google) alongside existing Geist fonts
- Add both font CSS variables to body className
- Import Header and Footer, render them wrapping {children}:
  ```
  <body>
    <Header />
    <main>{children}</main>
    <Footer />
  </body>
  ```
- Keep `antialiased` class on body

**page.tsx updates:**
- Replace the placeholder content with a simple temporary message like "Content loading..." or just render nothing (empty fragment). The actual sections will be built in Plans 02 and 03 and composed into page.tsx. For now, add section id anchors as empty divs so header nav links don't break:
  ```
  <div id="hero" />
  <div id="about" />
  <div id="how-it-works" />
  <div id="benefits" />
  <div id="pricing" />
  <div id="contact" />
  ```
  This ensures smooth-scroll targets exist. Actual content comes in Plans 02 and 03.
  </action>
  <verify>
Run `npm run build` -- must succeed with zero errors.
Run `npm run dev` and confirm:
1. Header renders at top with brand text and nav links
2. Footer renders at bottom with three columns and disclaimer
3. CTA button in header is visible
4. Scrolling works (nav links point to anchors)
5. No hydration errors in browser console
  </verify>
  <done>
- Header.tsx is a Server Component with sticky positioning, brand text, nav links (hidden on mobile), and CTA button
- Footer.tsx is a Server Component with 3-column layout, contact placeholders, and scientific disclaimer
- CTAButton.tsx is a Client Component with smooth scroll and 3 style variants
- layout.tsx has comprehensive SEO metadata (title, description, OG, Twitter), heading font loaded, Header + Footer wrapping main content
- page.tsx has section anchor divs for nav targets
- Build passes with zero errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. `npm run lint` passes with no errors
3. Dev server shows header with navigation and footer with disclaimer
4. Font families render correctly (serif for brand, sans for body)
5. CTA button smooth-scrolls when clicked
6. No 'use client' on Header.tsx or Footer.tsx (Server Components)
7. CTAButton.tsx has 'use client' directive
</verification>

<success_criteria>
- Design system tokens defined in globals.css via @theme (colors, fonts, spacing)
- Header with sticky nav and CTA visible on every page
- Footer with contact info and scientific disclaimer visible on every page
- SEO metadata set in layout.tsx (title, description, OG, Twitter)
- Build succeeds, lint passes, no hydration errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-content-seo/02-01-SUMMARY.md`
</output>
